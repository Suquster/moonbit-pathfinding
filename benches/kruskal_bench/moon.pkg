// Package: benches/kruskal_bench
//
// Smoke guard plus native `moon bench` workload for Kruskal's minimum
// spanning tree on a 1k-node / 10k-edge random graph.
import {
  "moonbitlang/core/bench",
  "Suquster/moonbit-pathfinding/src/undirected" @und,
}
