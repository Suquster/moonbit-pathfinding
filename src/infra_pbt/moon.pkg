// 包：Suquster/moonbit-pathfinding/src/infra_pbt
//
// 共享的属性测试（Property-Based Testing, PBT）基础设施。
// 为 moonbit-infra-suite 的全部方向提供统一的：
//   * 种子驱动确定性伪随机源 `Rng`（xorshift64，要求 wasm-gc / native / js
//     三后端逐位一致）；
//   * 生成器结构 `Gen[T]`；
//   * 高价值性质断言模板 `round_trip`（往返）与 `holds_for_all`（不变量）。
//
// 约定（Requirement 11.2 / 11.3）：
//   * 每条属性测试最少 100 次迭代（见 `default_iters`）；
//   * 测试注释统一标注 `Feature: moonbit-infra-suite, Property {n}: {text}`。
//
// 本包是横切的测试期共享约定，不依赖其它方向子包，避免引入运行时耦合。
